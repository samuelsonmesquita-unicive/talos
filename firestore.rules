rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir leitura e escrita para o sistema de gestão acadêmica Unicive
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
